namespace Needlr.Api;

/// <summary>Empty type used as a stable assembly anchor (e.g., <c>typeof(ApiAssemblyMarker).Assembly</c>)
/// for tests, MediatR/FluentValidation assembly scans, and <c>WebApplicationFactory&lt;&gt;</c>.
/// Survives template scaffold deletion so test projects don't break across phases.</summary>
public sealed class ApiAssemblyMarker;
